-- Multiple lines comment --
1. select the first caracter of your block
2. press Ctrl+V ( this is rectangular visual selection mode)
3. type j for each line more you want to be commented
4. type Shift-i (like I for "insert at start")
5. type // (or # or " or ...)
6. you will see the modification appearing only on the first line
7. IMPORTANT LAST STEP: type Esc key, and there you see the added character appear

-- Select inside brackets {xors} --
viB

-- Select inside parentheses (word) --
vib

-- Select betweein quotes 'quot' --
vi'

-- Next word on cursor (search) --
shift'

-- Select all --
ggVG

-- Delete --
Delete inside di and then any character ex di"
Delete word, dw
daw : delete the word under the cursor    
caw : delete the word under the cursor and put you in insert mode

-- Replace --
With cursor at word you can cycle with * then cgn 'write the replacement' esc and press . to change next
